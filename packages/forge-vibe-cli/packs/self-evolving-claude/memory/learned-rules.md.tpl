# Learned rules

Graduated from observations and corrections. Keep this file **short and testable**. Each bullet should be actionable; add **verify:** hints where machine checks make sense.

---

<!-- Example:
- Never do X in module Y.
  verify: Grep("pattern", path="src/") -> 0 matches
  [source: corrected 2x, {{DATE_ISO}}]
-->
