# TypeScript stack — {{PROJECT_NAME}}

- Prefer strict TypeScript; avoid `any` unless justified.
- Co-locate tests with source or use project `test/` convention consistently.
- Use the repo’s existing bundler/runtime (Node, Vite, Next, etc.)—do not switch stacks without explicit approval.
