# Pack: `quality_verification_layer` (reserved)

This directory exists as a **placeholder** for the future **FR42** default pack.

- **Not implemented** — no templates or generator wiring until the external quality-verification OSS product is generally available.
- The manifest lists this ID under **`reserved_pack_ids`** only.

Product documentation: `docs/FR42-quality-verification-layer.md` (repository root).
