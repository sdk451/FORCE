{
  "name": "forge-vibe-cli",
  "version": "0.1.0",
  "description": "BMAD-style installer for forge agent packs (Claude Code, Cursor, …)",
  "type": "module",
  "main": "./dist/cli.js",
  "bin": {
    "forge-vibe": "./dist/cli.js"
  },
  "files": [
    "dist",
    "packs"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
